agents:
  - id: "heating"
    name: "Heating Manager"
    model: "mistral:7b-instruct"
    decision_interval: 120
    entities: []  # populated from env vars for backward compatibility if needed, or specific list here
    instruction: |
      Maintain comfortable temperature (21C day, 18C night).
      Turn off if windows are open.
      
  - id: "cooling"
    name: "Cooling Manager"
    model: "mistral:7b-instruct"
    decision_interval: 120
    instruction: |
      Keep temperature below 25C. 
      Use fans first, then AC.
      
  - id: "lighting"
    name: "Lighting Manager"
    model: "phi3.5:3.8b"
    decision_interval: 60
    instruction: |
      Manage lights based on occupancy and time of day.
      Use warmer colors (2700K) in the evening.
      
  - id: "security"
    name: "Security Manager"
    model: "mistral:7b-instruct"
    decision_interval: 180
    # Publish events so other agents can react without re-polling HA.
    publishes:
      - "person_detected"
      - "door_opened"
    instruction: |
      Monitor locks and sensors.
      Lock doors if no motion for 30 mins.
      Report any anomalies.

  # Example: schedule-aware agent.
  # Only activates on weekday mornings; sleeps the rest of the time to
  # avoid burning LLM resources at 3 AM.
  #
  # - id: "morning_routine"
  #   name: "Morning Routine"
  #   model: "mistral:7b-instruct"
  #   decision_interval: 300
  #   schedule:
  #     active_hours: "06:00-09:00"
  #     days: ["mon", "tue", "wed", "thu", "fri"]
  #   instruction: "Gradually turn on lights and start the coffee maker."

  # Example: event-driven agent.
  # Wakes immediately when security publishes "person_detected" instead
  # of waiting for its own polling interval.
  #
  # - id: "porch_lighting"
  #   name: "Porch Lighting"
  #   model: "phi3.5:3.8b"
  #   decision_interval: 300
  #   listens_to:
  #     - "person_detected"
  #   entities:
  #     - light.porch
  #   instruction: "When a person is detected at night, illuminate the porch for 5 minutes."

  # Example: full cron expression for calendar-aware scheduling.
  #
  # - id: "energy_manager"
  #   name: "Energy Manager"
  #   model: "mistral:7b-instruct"
  #   decision_interval: 600
  #   schedule:
  #     cron: "0 18-22 * * *"   # active 18:00–22:00 every day
  #   instruction: "Shift non-critical loads to off-peak hours. Report current energy cost."

  # Example of a new custom agent
  # - id: "pool_manager"
  #   name: "Pool Guy"
  #   model: "mistral:7b-instruct"
  #   decision_interval: 3600
  #   entities: 
  #     - switch.pool_pump
  #     - sensor.pool_temp
  #   instruction: |
  #     Run the pool pump for 8 hours/day.
  #     Ensure temp is above 20C before heating.

  - id: "custom_agent"
    name: "Custom Agent"
    model: "mistral:7b-instruct"
    decision_interval: 120
    entities:
      - climate.ethan
    instruction: "I want you to monitor ethans room temp this shouold be 10 degrees all the time"
